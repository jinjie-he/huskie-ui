import { mount } from '@vue/test-utils'
import H{{hump name}} from '@huskie-ui/{{name}}'

describe('@huskie-ui/{{name}}', () => {
  it('create', () => {
    const wrapper = mount(H{{hump name}})
    expect(wrapper.classes()).toContain('h-{{name}}')
  })
})
