import { mount } from '@vue/test-utils'
import Huskie{{hump name}} from '@huskie-ui/{{name}}'

describe('@huskie-ui/{{name}}', () => {
  it('create', () => {
    const wrapper = mount(Huskie{{hump name}})
    expect(wrapper.classes()).toContain('huskie-{{name}}')
  })
})
